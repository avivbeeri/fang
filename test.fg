const y: i8 = 6;
// const s: string = "words";
fn main(): bool {
  var x: bool = test();
}
var x: i8;

fn test(): bool {
// x is actually not defined if this is called
  x + 5;
}


// printf text
/* asm {
"mov X0, #1"
"adr X1, const_0 + 1"
"adr X2, const_0"
"ldrb W2,[X2, 0]"
"mov X16, #4"
"svc 0"
}; */
