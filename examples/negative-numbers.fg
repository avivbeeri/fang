import "lib.fg"

fn main(): u8 {
  sys::writeI8(-54);
  sys::writeChar(' ');
  sys::writeI8(135); // Should display as negative
  sys::writeChar(' ');
  sys::writeI8(64 - 76); // Should display as negative
  return 0;
}
