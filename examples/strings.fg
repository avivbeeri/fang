import "lib.fg"
fn main(): void {
  const list: [5]string = [ "a", "b", "c", "d", "e"];
  const s: string = "hello world";
  for (var q: u8 = 0; q < 11; q = q + 1) {
    sys::writeChar(s[q]);
  }
  sys::writeChar(' ');
  for (var q: u8 = 0; q < 5; q = q + 1) {
    var s: string = list[q];
    sys::writeChar(s[0]);
  }
}

