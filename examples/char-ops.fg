import "lib.fg"

fn main(): void {
  var i: char = 'A';
  var j: char = 'J';
  var z: char = 'Z';
  if (i < j) {
    sys::writeChar('O');
  } else {
    sys::writeChar('X');
  }
  if (z > i) {
    sys::writeChar('O');
  } else {
    sys::writeChar('X');
  }
}
