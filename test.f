type Sprite {
  x: int8;
  y: int8;
  alive: bool;
}

fn aMethod(i: string, g: uint8): void {
  somethingElse(a, b, c);

  asm {
    ld a, b
  };
//  var a: Sprite = [ 0, 0, true ];
  var a: ptr = "words
  on
  new lines";
  @(a + 5) = Sprite;
  a.x = f;
  var y: uint8 = a.y;
  return;
}
