type Sprite {
  x: int8;
  y: int8;
  alive: bool;
}

fn aMethod(i: string, g: uint8) {
  somethingElse(a, b, c);
  //var a: Sprite = [ 0, 0, true ];
  var a: Sprite;
  a.x = f;
  var y: uint8 = a.y;
  return;
}
