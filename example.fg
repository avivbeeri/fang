fn syscall_write(c: char): void {
/*
  asm {
    "MOV X0, #1" // STDOUT
    "ADD X1, FP, #16" // PTR (FP+16) // function argument
    "MOV X2, 1"
    "MOV X16, #4"
    "SVC 0"
   };
*/
}

fn main(): i8 {
  var c: char = 'a';
//  for (var i: i8 = 0; i < (1 << 4); i = i + 1) {
//    syscall_write(c);
 // }
  return 0;
}

/*
fn write(c : char): void {
  syscall_write(c);
}


*/
